# 安全策略

## 支持范围

当前个人 Alpha 仅维护 `main` 分支的最新版本。尚未发布的历史提交不单独提供安全修复。

## 报告漏洞

请不要在公开 Issue 中提交 API Key、录音、关键帧、数据库、日志或可复现个人隐私的数据。

优先使用 GitHub 仓库的 Private vulnerability reporting 提交漏洞。报告应包含：

- 受影响版本或提交；
- 可复现步骤；
- 实际影响；
- 已确认不会泄露真实个人数据的最小示例。

如果 Private vulnerability reporting 暂不可用，请只创建不含敏感细节的 Issue，说明需要私下提供复现信息。
